import { type NextPage } from "next";
import { Container, Typography } from "@mui/material";

const Home: NextPage = () => {
    return (
        <Container>
            <Typography variant="h1">Parallel</Typography>
        </Container>
    );
};

export default Home;
